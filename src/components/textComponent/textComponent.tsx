import React from 'react'


interface props {
    page: JSX.Element
}

export const TextComponent: React.FunctionComponent<props> = ({page}: props): JSX.Element => {
    // return (
    //     <div>{page}</div>
    // )
    return (
        <div></div>
    )
}